<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

	<!-- 회원가입 -->
	<insert id="signup">
		INSERT INTO member ( user_id, pwd, nickname, email )
		VALUES(	#{user_id}, #{pwd}, #{nickname}, #{email} )
	</insert>

	<select id="idChk" resultType="int">
		select count(*) from member
		where user_id = #{user_id}
	</select>
	<select id="nameChk" resultType="int">
		select count(*) from member
		where nickname = #{nickname}
	</select>
	<select id="emailChk" resultType="int">
		select count(*) from member
		where email = #{email}
	</select>
	<select id="logincheck" resultType="com.doggle.vo.MemberVO">
		select user_id, pwd from member
		where user_id = #{user_id} and pwd = #{pwd}
	</select>

</mapper>