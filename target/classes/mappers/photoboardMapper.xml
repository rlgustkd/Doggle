<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="photoboardMapper">
<!-- 게시물 업로드 -->

<!-- 이미지 출력 -->
<select id="loadPosts" resultType="com.doggle.vo.PhotoboardVO">
    SELECT b_no, p_no, user_id, pb_title, pb_content, regdate, pb_photo, viewcnt, recommend, reportcnt, use_flag 
    FROM photoboard order by p_no desc
</select>
<insert id="uploadPost">
	INSERT INTO photoboard (p_no, user_id, pb_title, pb_content, pb_photo)
	VALUES (h_b_p_no.NEXTVAL, #{user_id}, #{pb_title}, #{pb_content}, #{pb_photo}) 
</insert>

</mapper>