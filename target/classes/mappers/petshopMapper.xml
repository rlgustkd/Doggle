<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="petshopMapper">

<!-- 이미지 업로드 -->
<insert id="saveImage" >
insert into petshop  (b_no, p_no, p_title, p_content, lng, lat, phone, p_photo, use_flag)
values (7,d_p_no_seq.nextval,'제목','내용',1,1,3123,#{p_photo},1)
</insert>

<!-- 이미지 출력 -->
<select id="getByteImage" resultType="com.doggle.vo.PetshopVO">
    SELECT b_no, p_no, p_title, p_content, lng, lat, phone, p_photo, use_flag
    FROM petshop
</select>

<!-- 추천목록 상세보기 이미지 출력 -->
<select id="detailImage" resultType="com.doggle.vo.PetshopVO">
	select b_no, p_no, p_title, p_content, lng, lat, phone, p_photo, use_flag
	from petshop
	where p_no = #{p_no}
</select>


</mapper>